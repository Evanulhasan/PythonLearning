# if__name__=="__main__": in python is used to check whether a Python script is being run directly or being imported as a module in another script. When a Python script is run directly, the special variable __name__ is set to "__main__". If the script is imported as a module, __name__ 
# will be set to the name of the module.


import oualid 

oualid.welcome()

# print(oualid.oualid)

# If the module is run directly, the following code will execute
# If the module is imported, the following code will not execute
# if__name__=="__main__":



























